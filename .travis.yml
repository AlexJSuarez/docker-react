sudo: required
services:
    - docker

before_install:
    - docker build -t candamir/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true candamir/docker-react npm run test -- --coverage